<h1>Articles</h1>
<br>

<ul>
<% @articles.each do | article | %>
  <li>
    <a href="<%= article_path(article) %> ">
      <h3><%= article.title %></h3>
    </a>
  </li>
<% end %>
</ul>

<a href="<%= new_article_path %>">New Article</a>
